version: '3'

services:

    base:
        container_name: sheng_ctf_1404
        image: sheng_ctf_1404:0.0
        ports:
          - "9453:22"
        build:
          context: ./
        volumes:
          - ./ctf:/root/ctf
        privileged: true
        hostname: sheng_ctf_1404
        restart: always
